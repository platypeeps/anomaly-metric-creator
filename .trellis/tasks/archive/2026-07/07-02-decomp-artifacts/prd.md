# Extract artifacts.py from legacy.py (decomposition step 4)

## Goal

Move _atomic_artifact_open, _atomic_write_text, and _ATOMIC_TMP_SUFFIX to artifacts.py. Verbatim move + legacy re-import per design.md.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
