# Move topology CSV helpers and lint run_tool boilerplate into conftest

## Goal

A-033 + A-037: conftest topology column helpers with SCENARIOS-derived exclusion windows, and a shared run_tool subprocess helper for the lint tests

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
