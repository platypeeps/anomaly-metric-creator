# Review Learnings

<!-- sd-review-learnings:start -->
## SD Review Learnings

_Last updated: 2026-08-08_

### Local Pattern Findings
- No local review-cycle findings detected in the scanned diff.

### Recent Copilot Review Signals

#### Current Actionable Comments
- **current** PR #325 `.trellis/tasks/08-03-sd-review-kb-coordinator-false-positive/design.md`: The citation `review.py:1706` is ambiguous/incorrect in this repo (no such file). Since this references the vendored coordinator implementation, cite `scripts/sd-ai-command-pack-review.py:1706` so readers can jump... (https://github.com/platypeeps/anomaly-metric-creator/pull/325)
- **current** PR #325 `docs/spec/amc/backend/sd-review-kb-false-block.md`: The source citation `review.py:1706` is ambiguous/incorrect in this repo (there is no `review.py` file). The vendored coordinator script is `scripts/sd-ai-command-pack-review.py`, so the reference should point there... (https://github.com/platypeeps/anomaly-metric-creator/pull/325)
- **current** PR #325 `.trellis/tasks/08-03-sd-review-kb-coordinator-false-positive/design.md`: The design doc refers to `update-spec-kb.py --check`, but the repo-local runnable command is `python3 scripts/sd-ai-command-pack-update-spec-kb.py --check`. Using the concrete command helps operators reproduce the... (https://github.com/platypeeps/anomaly-metric-creator/pull/325)
- **current** PR #325 `.trellis/tasks/08-03-sd-review-kb-coordinator-false-positive/research/root-cause.md`: The citation `review.py:1706` doesn't correspond to any file in this repo (and is ambiguous for readers). Since the investigation is tracing the vendored coordinator, point at the actual script path here... (https://github.com/platypeeps/anomaly-metric-creator/pull/325)
- **current** PR #325 `docs/spec/amc/backend/sd-review-kb-false-block.md`: The `.gitignore` source citation is written as `/.gitignore:...`, but the file in this repo is `.gitignore` (no leading slash). Also, `update-spec-kb.py --check` is ambiguous here—elsewhere the runbook uses the... (https://github.com/platypeeps/anomaly-metric-creator/pull/325)
- **current** PR #325 `.trellis/tasks/08-03-sd-review-kb-coordinator-false-positive/prd.md`: This requirement still frames the coordinator KB source-of-truth as an open question (live worktree vs `/tmp` snapshot), but the correction above says the snapshot hypothesis is already refuted and the live-worktree... (https://github.com/platypeeps/anomaly-metric-creator/pull/325)
- **current** PR #292 `.trellis/workspace/sdelmas/index.md`: The Active Documents table’s line count for `journal-1.md` is out of sync with the actual file length (journal currently reaches line 1685). This table is auto-maintained by `.trellis/scripts/add_session.py` and... (https://github.com/platypeeps/anomaly-metric-creator/pull/292)
- **current** PR #252 `src/anomaly_metric_creator/validate_impl.py`: Violation.from_message() only recognizes a narrow set of row-level message shapes. The new cell validator can emit messages like "<component>.csv line N: <metric>=... not parseable as float" and "<component>.csv... (https://github.com/platypeeps/anomaly-metric-creator/pull/252)
- **current** PR #232 `scripts/sd-ai-command-pack-shell-lib.sh`: `delay=$((delay * 2))` will error in Bash when `delay` contains a leading zero (e.g. an env var set to `08`), because arithmetic expansion treats such values as octal. That can crash the retry/backoff loop on... (https://github.com/platypeeps/anomaly-metric-creator/pull/232)
- **current** PR #229 `src/anomaly_metric_creator/legacy.py`: Same as the aggregate path: if sum_w <= 0 here, we currently still proceed to generate and apply a coupling array (including noise) for this metric, which contradicts the stated intent that zero total constant... (https://github.com/platypeeps/anomaly-metric-creator/pull/229)
- **current** PR #229 `tests/test_topology_finite.py`: This monkeypatch sets the edge weight to 0.0, but the coupling code filters out `w == 0.0` edges before building `active_constant`, so the test never reaches the `w / sum_w` normalization site (and won't fail... (https://github.com/platypeeps/anomaly-metric-creator/pull/229)
- **current** PR #229 `src/anomaly_metric_creator/legacy.py`: When sum_w <= 0, this branch skips the normalization loop but still proceeds to override the metric with `coupled = constant_contrib + ... + rng.normal(...)`, which means a zero/invalid normalization produces a... (https://github.com/platypeeps/anomaly-metric-creator/pull/229)
- **current** PR #227 `scripts/sd-ai-command-pack-full-check.sh`: `join_by_comma ${patterns[@]+"${patterns[@]}"}` is unquoted, so paths/patterns containing whitespace will be split into multiple arguments (and glob characters could expand). That can generate an incorrect... (https://github.com/platypeeps/anomaly-metric-creator/pull/227)
- **current** PR #227 `scripts/sd-ai-command-pack-record-session.py`: For `git status --porcelain -z`, rename/copy entries are emitted as `R... <old>\0<new>\0` (old path first, new/current path second). The current loop keeps `path_text` from the first token and then skips the second... (https://github.com/platypeeps/anomaly-metric-creator/pull/227)
- **current** PR #227 `scripts/sd-ai-command-pack-review-local.sh`: `join_by_comma ${patterns[@]+"${patterns[@]}"}` is unquoted, so any path/pattern containing whitespace will be split into multiple arguments (and glob characters could expand). This can produce an incorrect... (https://github.com/platypeeps/anomaly-metric-creator/pull/227)
- **current** PR #226 `scripts/sd-ai-command-pack-full-check.sh`: Same quoting issue as in `sd-ai-command-pack-review-local.sh`: this unquoted array expansion will split elements containing spaces, producing an incorrect filter CSV. Quote the `${patterns[@]+...}` expansion so each... (https://github.com/platypeeps/anomaly-metric-creator/pull/226)
- **current** PR #226 `scripts/sd-ai-command-pack-record-session.py`: `git status --porcelain -z` emits two NUL-delimited path tokens for rename/copy entries. The code currently keeps the first token’s path (which is the *source* side) and merely skips the second, so the journal... (https://github.com/platypeeps/anomaly-metric-creator/pull/226)
- **current** PR #226 `scripts/sd-ai-command-pack-review-local.sh`: `join_by_comma` is invoked with an unquoted array expansion. If any reviewed path/pattern contains whitespace, the unquoted `${patterns[@]+...}` will undergo word-splitting and produce an incorrect CSV (and... (https://github.com/platypeeps/anomaly-metric-creator/pull/226)

#### Historical Signal Clusters
- **Other recurring signals** (`other`): 51 historical comment(s) across 51 normalized signature(s); PRs #240, #242, #254, #255, #267, #270, #271, #274; path families `.trellis/audit`, `.trellis/scripts`, `.trellis/workspace`, `github-config`, `repository-root`, `src/anomaly_metric_creator`; observed 2026-07-14 to 2026-08-08.
  - Representative signatures: `This section advises writing exclusive “If X / If not X” criteria and then checking *both* boxes with an...` (x1); `` `server_traces.py` currently has 1087 physical lines (it ends with a trailing blank line), but the... `` (x1); `The docstring’s explanation of the “over its ratchet ceiling” failure mode says “a module already 5x the...` (x1); `` This comment says pre-commit passes matched filenames, but the new pre-commit hook sets `pass_filenames:... `` (x1)
  - Example: PR #361 `docs/spec/amc/backend/documentation-review.md`: This section advises writing exclusive “If X / If not X” criteria and then checking *both* boxes with an explicit unreachable-branch note, but the worked... (https://github.com/platypeeps/anomaly-metric-creator/pull/361)
  - Example: PR #360 `tools/check_module_size.py`: `server_traces.py` currently has 1087 physical lines (it ends with a trailing blank line), but the module-size ratchet ceiling here is set to 1086. As... (https://github.com/platypeeps/anomaly-metric-creator/pull/360)
  - Example: PR #359 `tools/check_module_size.py`: The docstring’s explanation of the “over its ratchet ceiling” failure mode says “a module already 5x the cap”, but enrolled modules can be only modestly... (https://github.com/platypeeps/anomaly-metric-creator/pull/359)
  - _Evidence truncated: signatures 4/51, PRs 8/27, path families 6/13, examples 3/51._
- **Task metadata** (`task-metadata`): 50 historical comment(s) across 34 normalized signature(s); PRs #246, #248, #250, #274, #279, #310, #317, #332; path families `documentation`, `task-metadata`; observed 2026-07-17 to 2026-08-07.
  - Representative signatures: `` `base_branch` is set to the temporary audit backlog branch (`chore/audit-2026-07-17-backlog`). Trellis... `` (x10); `` `base_branch` is set to `chore/audit-2026-07-17-backlog`, which is the current PR branch and is likely to... `` (x7); `This jsonl file still contains the scaffold placeholder line telling the reader to delete it once real...` (x2); `The updated description says it "cannot track the epic's own children as they land", but it also includes...` (x1)
  - Example: PR #246 `.trellis/tasks/07-17-audit-mcp-query-performance/task.json`: `base_branch` is set to the temporary audit backlog branch (`chore/audit-2026-07-17-backlog`). Trellis treats `base_branch` as the PR target (see... (https://github.com/platypeeps/anomaly-metric-creator/pull/246)
  - Example: PR #246 `.trellis/tasks/07-17-audit-doc-accuracy-sweep/task.json`: `base_branch` is set to `chore/audit-2026-07-17-backlog`, which is the current PR branch and is likely to be deleted after merge. Since Trellis uses... (https://github.com/platypeeps/anomaly-metric-creator/pull/246)
  - Example: PR #332 `.trellis/tasks/08-04-server-helm-impl-extract/check.jsonl`: This jsonl file still contains the scaffold placeholder line telling the reader to delete it once real entries are added. Since this task is being checked... (https://github.com/platypeeps/anomaly-metric-creator/pull/332)
  - _Evidence truncated: signatures 4/34, PRs 8/12, examples 3/50._
- **Contract/documentation drift** (`contract-documentation-drift`): 32 historical comment(s) across 32 normalized signature(s); PRs #240, #255, #263, #267, #271, #275, #276, #277; path families `.trellis/workspace`, `documentation`, `repository-root`, `scripts`, `src/anomaly_metric_creator`, `task-metadata`; observed 2026-07-14 to 2026-08-08.
  - Representative signatures: `` The spec says `tools/check_module_size.py` "counts what `wc -l` counts", but the tool’s own docstring is... `` (x1); `` `optional_keys` duplicates the optional-key list that the preceding tests already derive from... `` (x1); `` The `--list` example claims this prints a per-module table, but `_report()` only prints enrolled modules... `` (x1); `` `tools/check_ci_review_contract.py` now pins that the task-criteria guard runs, but it doesn't pin *how... `` (x1)
  - Example: PR #361 `docs/spec/amc/backend/testing-quality.md`: The spec says `tools/check_module_size.py` "counts what `wc -l` counts", but the tool’s own docstring is more precise: it matches `wc -l` only for... (https://github.com/platypeeps/anomaly-metric-creator/pull/361)
  - Example: PR #360 `tests/test_server.py`: `optional_keys` duplicates the optional-key list that the preceding tests already derive from `TracePayload` via `_trace_payload_key_split()`. Keeping a... (https://github.com/platypeeps/anomaly-metric-creator/pull/360)
  - Example: PR #357 `tools/check_module_size.py`: The `--list` example claims this prints a per-module table, but `_report()` only prints enrolled modules plus the top 5 under-cap modules. Either print all... (https://github.com/platypeeps/anomaly-metric-creator/pull/357)
  - _Evidence truncated: signatures 4/32, PRs 8/27, path families 6/12, examples 3/32._
- **Boundary validation** (`boundary-validation`): 28 historical comment(s) across 28 normalized signature(s); PRs #248, #249, #252, #254, #256, #257, #268, #269; path families `.sd-ai-command-pack/manifest.json`, `.trellis/workspace`, `documentation`, `scripts`, `src/anomaly_metric_creator`, `task-metadata`; observed 2026-07-17 to 2026-08-06.
  - Representative signatures: `If the tool’s stderr message is updated to clarify the tracked-only discovery (recommended), this...` (x1); `This spec says server_k8s_api.py imports one-way from server_traces, but the extracted module does not...` (x1); `_window_boundary_strings() can raise OverflowError for extreme 'from_ms'/'to_ms' values because it...` (x1); `` The new `--otel-stream-auth-scheme` row is malformed (unclosed backtick / missing closing cell... `` (x1)
  - Example: PR #343 `tests/test_trellis_placeholder_lint.py`: If the tool’s stderr message is updated to clarify the tracked-only discovery (recommended), this assertion should match the new wording so the test... (https://github.com/platypeeps/anomaly-metric-creator/pull/343)
  - Example: PR #341 `docs/spec/amc/backend/architecture.md`: This spec says server_k8s_api.py imports one-way from server_traces, but the extracted module does not import server_traces (it imports DEFAULT_NAMESPACE,... (https://github.com/platypeeps/anomaly-metric-creator/pull/341)
  - Example: PR #337 `src/anomaly_metric_creator/server_mcp.py`: _window_boundary_strings() can raise OverflowError for extreme 'from_ms'/'to_ms' values because it constructs datetimes via epoch arithmetic. That... (https://github.com/platypeeps/anomaly-metric-creator/pull/337)
  - _Evidence truncated: signatures 4/28, PRs 8/20, path families 6/10, examples 3/28._
- **Generated surfaces** (`generated-surfaces`): 17 historical comment(s) across 17 normalized signature(s); PRs #242, #243, #298, #306, #314, #315; path families `.agents/skills`, `.opencode/skills`, `.sd-ai-command-pack/manifest.json`, `scripts`; observed 2026-07-14 to 2026-08-04.
  - Representative signatures: `` `_load_source_module()` collapses several path-policy failures (including platforms without... `` (x1); `` `PACK_FILE_PATTERNS` now includes `.claude/skills/sd-*/*`, but the new skill surface also installs nested... `` (x1); `` `is_trellis_runtime_path` now includes `.claude/settings.json`, but still omits `.gemini/settings.json`... `` (x1); `The PR title says the pack is updated to 0.30.4, but the installed manifest/provenance in this diff is...` (x1)
  - Example: PR #315 `scripts/sd-ai-command-pack-surface-check.py`: `_load_source_module()` collapses several path-policy failures (including platforms without `os.O_NOFOLLOW`) into the error message "missing source... (https://github.com/platypeeps/anomaly-metric-creator/pull/315)
  - Example: PR #314 `scripts/sd-ai-command-pack-install-audit.py`: `PACK_FILE_PATTERNS` now includes `.claude/skills/sd-*/*`, but the new skill surface also installs nested reference files like... (https://github.com/platypeeps/anomaly-metric-creator/pull/314)
  - Example: PR #306 `scripts/sd-ai-command-pack-review-scope.sh`: `is_trellis_runtime_path` now includes `.claude/settings.json`, but still omits `.gemini/settings.json` even though the repo contains... (https://github.com/platypeeps/anomaly-metric-creator/pull/306)
  - _Evidence truncated: signatures 4/17, examples 3/17._
- _Historical clusters truncated: showing 5 of 6 categories._

### Suggested Preventive Actions
- **Task metadata** (50 historical comments): Add a deterministic task-metadata validation gate before implementation or publication.
- **Contract/documentation drift** (32 historical comments): Add contract terminology checks that keep documentation and help text aligned with shipped behavior.
- **Boundary validation** (28 historical comments): Add boundary and failure-matrix fixtures for externally derived paths and states.
- **Generated surfaces** (17 historical comments): Extend source-to-generated parity checks for every affected shipped surface.
<!-- sd-review-learnings:end -->
